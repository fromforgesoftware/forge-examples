replicaCount: 2

database:
  host: '' # set by the deploy pipeline / env overlay
  name: catalog
  schema: catalog
  ssl: require
  existingSecret: catalog-db

aegisIssuer: 'http://aegis/realms/petstore'

autoscaling:
  enabled: false

podDisruptionBudget:
  enabled: true
  minAvailable: 1
