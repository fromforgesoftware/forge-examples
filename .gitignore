/dist/
*.out

# Go build outputs (e.g. `go build` dropping a binary in a package dir)
/petstore
/examples/petstore/petstore
/examples/petstore/catalog
/examples/petstore/adoptions

# Helm dependency build artifacts (pulled/packaged subcharts + lock)
**/charts/*.tgz
**/Chart.lock
